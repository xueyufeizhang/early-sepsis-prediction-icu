"""Stage 4 — Models (brief Section 6; Module 3).

classic.py  Day 6-7  LR, SVM, RandomForest, XGBoost, MLP  (static features)
deep.py     Day 8     LSTM                                 (hourly time series)

Each model: hyperparameter tuning (grid/random search) over patient-grouped
CV folds; report the best configuration. CNN/autoencoder are intentionally
NOT in the main track (no natural image/grid modality) — see brief Section 6.
"""
